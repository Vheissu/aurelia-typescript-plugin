import { expect } from "chai"

describe("Placeholder test", () => {
    beforeEach(() => {

    })

    describe("Placeholder test", () => {
        expect(1).to.equal(1)
        expect('test').to.equal('test')
        expect(false).to.be.false
    })

    afterEach(() => {

    })
})